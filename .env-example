# Default number of object per page
PER_PAGE = 9

# Django secret Key

# code to generate secret key

# python -c
# "import string as s;from random import SystemRandom as
# sr;print(''.join(sr().choices(s.ascii_letters +
# s.punctuation, k=64)))"

SECRET_KEY = 'CHANGE-ME'

# 0 = False - 1 = True
DEBUG = 1

# 0 = False - 1 = True - 0 shows the browser and 1 doesnot the browser
SELENIUM_HEADLESS = 1


# Database settings
# Sqlite
DATABASE_ENGINE = 'django.db.backends.sqlite3'
DATABASE_NAME = "./db.sqlite3"


# Postgres
# DATABASE_ENGINE = 'django.db.backends.postgresql'
# DATABASE_NAME = "basededados"

DATABASE_USER = "usuario"
DATABASE_PASSWORD = "senha"
DATABASE_HOST = "127.0.0.1"
DATABASE_PORT = "5432"
